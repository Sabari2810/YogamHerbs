/** @type {import('next').NextConfig} */
const nextConfig = {
    // experimental: {
    //     serverActions: true
    // },
    images: {
        domains: ["www.themancompany.com"]
    }
}

module.exports = nextConfig
